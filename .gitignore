node_modules
.next
data/*.log

# runtime data (simulated account state, journals, caches) — local only
data/

# phase 3 sandboxes / test data
tmp-test-data/
tmp-live-probe/

# TypeScript incremental build info
*.tsbuildinfo

# local agent / editor state (not part of the project)
.workbuddy-ai/

# OS noise
.DS_Store
Thumbs.db
